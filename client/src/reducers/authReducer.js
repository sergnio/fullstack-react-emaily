// this file begins with a lower case because we are exporting a function
// 1st arg, state object responsible for this reducer
// assume initial state is empty
// 2nd arg, an action
export default function(state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
}
